# Zoho Catalyst CLI Setup Report

**Installation Status**: 🟢 Successful
**CLI Version**: `1.26.0`

### Verification Results
* The `zcatalyst-cli` package was successfully installed globally via `npm`. 
* The executable was verified by bypassing the initial telemetry prompt and successfully executing a `--version` check.

### System Configuration
* **Executable Path**: `C:\Users\asus\AppData\Roaming\npm\catalyst.cmd`
* *Note*: If the `catalyst` command is not recognized natively in your terminal, you will need to add `%APPDATA%\npm` to your system's `PATH` environment variable.

### Next Steps
The CLI is now ready for authentication. 

**Next command to run:**
```bash
catalyst login
```
